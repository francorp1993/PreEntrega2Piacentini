// // Evento change en el select de género
// selectGenero.addEventListener('change', (event) => {
//     const id_genero = event.target.value;
//     filtroGenero(id_genero);
//     ultimoGeneroSeleccionado = id_genero; // Actualizar el último género seleccionado
// });

// // Función para filtrar lanzamientos por género
// const filtroGenero = (id_genero) => {
//     if (id_genero === "Todos") {
//         renderLanzamientos(lanzamientos_lista);
//     } else {
//         const nuevaLista = lanzamientos_lista.filter((lanzamiento) => lanzamiento.id_genero === parseInt(id_genero));
//         renderLanzamientos(nuevaLista);
//     }
// };

// // Función para renderizar los lanzamientos al iniciar
// const renderizarLanzamientosIniciales = () => {
//     getLanzamientos(); // Obtener lanzamientos iniciales
//     filtroGenero("Todos"); // Mostrar todos los lanzamientos al inicio
// };

// // Llamar a la función para renderizar lanzamientos al iniciar
// renderizarLanzamientosIniciales();